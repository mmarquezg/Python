Programa que permite al usuario crear una lista de un tamaño X e identifica el primer elemento repetido en la misma.

El mismo código recorre la lista creada por el usuario y almacena en dos nuevas listas los elementos duplicados y no duplicados.

En caso de no haber elementos duplicados, el programa nos dice que no hay duplicidad de Elementos